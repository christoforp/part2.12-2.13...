import ReactDOM  from "react";
import React from "react";

import App from "./part1/src/App";

const server = http.createrServer((reg, res) => {

})

server.listen(3005, () => {
    console.log('Server started at 3005');
   });
  
ReactDOM.render(<App />, document.getElementById('root')) 

